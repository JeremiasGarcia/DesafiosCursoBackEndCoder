<div class="container mt-5 mb-5>
    <h2>Vista de Productos Random</h2>

    {{#if prods}}
        <div class="table-responsive">
            <table class="table table-dark" id="listaProductos">
                <tr style="color: yellow;"> <th>Nombre</th> <th>Precio</th> <th>Foto URL</th> </tr>
                {{#each prods}}
                <tr>
                    <td>{{this.nombre}}</td>
                    <td>{{this.precio}}</td>
                    <td>
                        <img width="40" src="{{this.fotoUrl}}" alt="">
                    </td>
                </tr>
                {{/each}}
            </table>
        </div>
    {{else}}
    <h3 class="alert alert-warning">No se encontraron productos<h3>
    {{/if}}
</div>